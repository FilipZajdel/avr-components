set(CMAKE_C_COMPILER avr-gcc)

set(TIMERS_INCLUDE "${CMAKE_CURRENT_SOURCE_DIR}" PARENT_SCOPE)

set_property(GLOBAL PROPERTY TIMERS_PROPERTY "${CMAKE_CURRENT_SOURCE_DIR}")

add_library(timerslib timers.c)
